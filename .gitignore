# dependencies
node_modules/

# build
.next/

# claude
.claude/

*.tsbuildinfo

# env (시크릿 — 절대 커밋 금지)
.env
.env.local
.env*.local
.vercel
next-env.d.ts
.gstack/

# 검증 산물(도현/준 일시 프로브 — 커밋 대상 아님)
tmp/

# Cloudflare Workers — 별도 npm 설치. 메인 프로젝트와 분리.
workers/*/node_modules/
workers/*/wrangler.toml.bak
.dev.vars
