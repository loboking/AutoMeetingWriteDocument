// IA(정보구조도) 템플릿
export function getIaPrompt(baseInfo: string, transcript: string): string {
  return `당신은 정보 아키텍트입니다. 다음 회의 내용을 바탕으로 **IA(정보구조도) 문서**를 작성해주세요.

${baseInfo}

## 원본 회의 내용 (분석 대상)
${transcript}

---

## 작성 가이드

**중요**: 위 회의 내용에서 실제 논의된 정보 구조를 **추출**하여 작성하세요.
회의 내용에 없는 내용을 지어내지 말고, 논의된 구조를 바탕으로 체계화하세요.

## IA(정보구조도) 문서 구조

### 1. 정보 구조 개요
- **구조 유형**: 계층형 / 태그형 / 순차형
- **깊이**: 3단계
- **범위**: 전체 서비스

### 2. 사이트맵 (Site Map)

\`\`\`mermaid
graph TD
    A[홈] --> B[로그인]
    A --> C[회원가입]
    D[대시보드] --> E[위젯]
    D --> F[리포트]
    D --> G[설정]
    G --> G1[프로필]
    G --> G2[알림]
    G --> G3[계정]
\`\`\`

### 3. 네비게이션 구조

#### 3.1 글로벌 네비게이션
- 홈
- 대시보드
- 리포트
- 설정
- 로그아웃

#### 3.2 로컬 네비게이션
**대시보드 내부**:
- 위젯 관리
- 필터
- 새로고침

**설정 내부**:
- 프로필
- 알림
- 계정
- 결제

### 4. 카테고리 체계

#### 4.1 1단계 카테고리
| 카테고리 | 설명 |
|----------|------|
| 대시보드 | 메인 화면 |
| 리포트 | 데이터 리포트 |
| 설정 | 사용자 설정 |

#### 4.2 2단계 카테고리
| 상위 | 하위 | 설명 |
|------|------|------|
| 대시보드 | 위젯 | 위젯 관리 |
| 대시보드 | 필터 | 데이터 필터 |
| 설정 | 프로필 | 사용자 프로필 |
| 설정 | 알림 | 알림 설정 |
| 설정 | 계정 | 계정 관리 |

### 5. 콘텐츠 모델

#### 5.1 콘텐츠 타입
| 타입 | 설명 | 속성 |
|------|------|------|
| 위젯 | 대시보드 위젯 | 타입, 제목, 데이터소스 |
| 리포트 | 데이터 리포트 | 제목, 생성일, 데이터 |
| 사용자 | 사용자 정보 | 이름, 이메일, 역할 |

#### 5.2 콘텐츠 관계
\`\`\`mermaid
erDiagram
    USER ||--o{ WIDGET : creates
    USER ||--o{ REPORT : generates
    WIDGET }o--|| DATASOURCE : uses
\`\`\`

### 6. 라벨링 가이드

| 구분 | 용어 | 설명 |
|------|------|------|
| 화면명 | 대시보드 | 메인 대시보드 |
| 버튼 | 새로고침 | 데이터 갱신 |
| 메뉴 | 설정 | 환경 설정 |

회의 내용을 바탕으로 체계적인 정보구조를 설계하세요.`;
}
